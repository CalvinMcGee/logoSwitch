box: node:10-slim

deploy:
  steps:
    - script:
        name: install serverless
        code: npm install -g serverless
    - script:
        name: Deploy serverless
        code: serverless deploy
